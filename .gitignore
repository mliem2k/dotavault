.superpowers/
docs/superpowers/
.claude/worktrees/
node_modules
dist
.env
.env.local
*.local
.turbo
.vercel
.tmp/
.tanstack/
.worktrees/

# `go build` with no -o drops the binary next to the source, named after the
# module directory. It has been accidentally staged more than once.
apps/replay-parser/replay-parser
